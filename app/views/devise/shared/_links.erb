<%- if controller_name != 'sessions' %>
  Don't have an account? <strong><%= link_to "Log in", new_session_path(resource_name) %></strong> now.
<% end -%>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  Already have an account? <strong><%= link_to "Sign up", new_registration_path(resource_name) %></strong> now.
<% end -%>